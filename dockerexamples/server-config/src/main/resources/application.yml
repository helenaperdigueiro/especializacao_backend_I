server:
  port: 8888

spring:
  application:
    name: config-server
  zipkin:
    base-url: http://zipkin-service:9411/
  cloud:
    config:
      server:
        git:
          uri: https://github.com/RenatadsLeal/backend2
            default-label: main
            search-paths:
              - 'cloud-config*'

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-service:8761/eureka/