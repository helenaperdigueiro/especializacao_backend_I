server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/helenaperdigueiro/especializacao_backend_I
          default-label: main
          search-paths:
            - 'config-cloud*'